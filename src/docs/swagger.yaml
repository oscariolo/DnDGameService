openapi: 3.0.3
info:
  title: Game Service API
  description: API for managing DnD game sessions
  version: 1.0.0

servers:
  - url: http://localhost:3001
    description: Local dev server

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

paths: {}
